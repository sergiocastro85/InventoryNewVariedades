services:
  - type: web
    name: InventoryNewVariedades
    env: docker
    branch: master
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ConnectionStrings_DefaultConnection
        sync: false